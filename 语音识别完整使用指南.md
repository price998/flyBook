# 语音识别完整使用指南

## 🎤 功能特性

### ✨ 新功能
1. **上滑取消录音** - 长按时上滑可取消录音
2. **自动切换键盘模式** - 识别成功后自动显示输入框
3. **智能回退机制** - 科大讯飞失败时自动使用 Google 识别

## 📱 使用方法

### 1. 切换到语音模式
- 点击输入框右侧的 **麦克风图标** 🎤
- 界面会显示 "按住说话" 按钮

### 2. 语音识别操作

#### 🎯 正常录音流程
```
1. 长按 "按住说话" 按钮
   ↓
2. 按钮变为 "松开发送，上滑取消"（蓝色背景）
   ↓
3. 对着麦克风说话
   ↓
4. 松开按钮
   ↓
5. 自动切换到键盘模式
   ↓
6. 识别结果显示在输入框
   ↓
7. 可以编辑后点击发送
```

#### ❌ 取消录音流程
```
1. 长按 "按住说话" 按钮
   ↓
2. 开始说话
   ↓
3. 向上滑动手指（超过 150 像素）
   ↓
4. 按钮变为 "松开手指，取消发送"（红色背景）
   ↓
5. 松开手指
   ↓
6. 显示 "已取消录音"
   ↓
7. 识别结果被丢弃
```

## 🎨 视觉反馈

| 状态 | 按钮文字 | 背景颜色 | 说明 |
|------|---------|---------|------|
| 待机 | "按住说话" | 透明 | 等待用户操作 |
| 录音中 | "松开发送，上滑取消" | 浅蓝色 (#E3F2FD) | 正在录音 |
| 取消状态 | "松开手指，取消发送" | 浅红色 (#FFCDD2) | 上滑取消 |

## 🔄 两个页面的不同行为

### DialogueActivity（首页）
- **识别后**：结果显示在输入框
- **适用场景**：可以修改识别内容后再发送
- **保持模式**：始终在键盘模式

### ChatActivity（对话页）
- **识别后**：自动切换到键盘模式，结果显示在输入框
- **适用场景**：可以查看和编辑识别内容
- **模式切换**：语音模式 → 自动切换到键盘模式

## ⚙️ 技术细节

### 识别引擎
- **优先使用**：科大讯飞语音识别（更准确，支持离线）
- **备用方案**：Google 语音识别（兼容性好）

### 参数配置
```kotlin
// 前端点超时：4秒（开始说话前的静音时长）
VAD_BOS = 4000ms

// 后端点超时：1秒（停止说话后的静音时长）
VAD_EOS = 1000ms

// 语言：中文（普通话）
LANGUAGE = zh_cn
ACCENT = mandarin

// 标点符号：自动添加
ASR_PTT = 1
```

### 上滑取消阈值
- **触发距离**：150 像素
- **检测方式**：实时监测手指移动距离
- **取消方法**：调用 `xunfeiRecognizer?.cancel()`

## 🐛 常见问题

### 问题 1：长按没有反应

**检查清单**：
- [ ] 是否授予录音权限
- [ ] 是否在语音模式（不是键盘模式）
- [ ] 查看 Logcat 是否有错误

**解决方法**：
```
1. 检查权限：设置 → 应用 → 权限 → 麦克风
2. 查看日志：Logcat 搜索 "XunfeiSpeechRecognizer"
3. 重启应用
```

### 问题 2：识别结果不显示

**原因**：
- 可能被取消了（上滑超过阈值）
- 识别结果为空

**解决方法**：
- 确保松开时手指在原位（不要上滑）
- 在安静环境下测试
- 查看日志确认是否有识别结果

### 问题 3：无法切换到键盘模式

**原因**：
- 识别被取消
- 识别结果为空

**解决方法**：
- 只有识别成功且结果不为空时才会自动切换
- 可以手动点击麦克风图标切换

## 📊 日志查看

### 关键日志标签
```
XunfeiSpeechRecognizer  - SDK 相关日志
ChatActivity            - 对话页面日志
DialogueActivity        - 首页日志
```

### 成功识别日志示例
```
D/XunfeiSpeechRecognizer: 开始识别
D/XunfeiSpeechRecognizer: 开始说话
D/XunfeiSpeechRecognizer: 停止识别
D/XunfeiSpeechRecognizer: 识别结果：{"sn":1,"ls":true,...}
D/ChatActivity: 识别结果：你好
Toast: 识别成功
```

### 取消录音日志示例
```
D/XunfeiSpeechRecognizer: 开始识别
D/XunfeiSpeechRecognizer: 取消识别
Toast: 已取消录音
```

## 💡 使用技巧

### 1. 快速录音
- 按住说话，说完立即松开
- 无需等待，系统会自动处理

### 2. 取消录音
- 说错了？向上滑动手指即可取消
- 看到红色背景就表示会取消

### 3. 编辑识别结果
- 识别成功后会自动切换到键盘模式
- 可以在输入框中编辑识别的文字
- 编辑完成后点击发送按钮

### 4. 连续语音输入
- 发送后，点击麦克风图标切换回语音模式
- 继续长按录音

## 🎯 最佳实践

### 环境要求
- ✅ 安静环境
- ✅ 距离麦克风 10-30cm
- ✅ 发音清晰
- ❌ 避免背景噪音
- ❌ 避免口头禅

### 说话技巧
- 语速适中，不要太快
- 停顿明确，便于断句
- 避免"嗯"、"啊"等语气词

### 网络要求
- 科大讯飞在线模式需要网络
- Google 识别必须有网络
- 建议在 WiFi 环境下使用

## 🔧 高级配置

### 启用离线识别
如需完全离线使用：
1. 下载科大讯飞离线资源包
2. 放置到 `app/src/main/assets/iflytek/`
3. 修改 `XunfeiSpeechRecognizer.kt` 配置

### 调整识别参数
可以在 `XunfeiSpeechRecognizer.kt` 中调整：
- `VAD_BOS`：前端点超时（说话前静音时长）
- `VAD_EOS`：后端点超时（说话后静音时长）
- `ACCENT`：方言选择

## 📈 性能优化

### 已实现的优化
- ✅ 主线程 UI 更新
- ✅ 资源自动释放
- ✅ 生命周期管理
- ✅ 智能回退机制

### 内存管理
- Activity 销毁时自动释放识别器
- 避免内存泄漏
- 及时取消未完成的识别

---

**提示**：如有问题，请查看 Logcat 日志或参考《科大讯飞SDK配置步骤.md》
