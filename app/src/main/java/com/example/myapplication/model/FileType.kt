package com.example.myapplication.model

/**
 * 文件类型枚举
 */
enum class FileType {
    IMAGE,      // 图片（JPG, PNG等）
    PDF,        // PDF文档
    WORD,       // Word文档（.doc, .docx）
    UNSUPPORTED // 不支持的文件类型
}
