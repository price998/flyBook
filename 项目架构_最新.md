# 项目架构文档（最新版）

> 更新时间：2024年12月10日  
> 项目架构：3个Activity + 2个Fragment + MVVM  
> 总文件数：53个核心代码文件

---

## 一、项目概述

### 1.1 架构模式

- **MVVM架构**：Model-View-ViewModel
- **模块化设计**：按功能划分模块，每个模块包含完整的MVVM结构
- **Fragment组件化**：2个核心Fragment在多个Activity中复用
- **响应式编程**：LiveData + Kotlin Coroutines + Flow

### 1.2 核心结构（3个Activity + 2个Fragment）

#### Activity（4个）
1. **LoginActivity**：用户登录/注册
2. **MainActivity**：主页（推荐话题 + 输入栏 + 侧边栏）
3. **ChatActivity**：聊天对话（消息列表 + 输入栏 + 侧边栏）
4. **SearchActivity**：搜索历史对话

#### Fragment（2个）
1. **HistoryFragment**：历史对话列表（在MainActivity和ChatActivity的侧边栏中复用）
2. **InputBarFragment**：输入栏（在MainActivity和ChatActivity中复用）

### 1.3 技术栈

- **Kotlin**: 1.9.24
- **Android SDK**: minSdk 24, targetSdk 34
- **Room**: 2.6.1（数据库）
- **OkHttp**: 4.12.0（网络请求）
- **Coroutines**: 1.8.1（协程）
- **Lifecycle**: 2.8.7（ViewModel、LiveData）
- **Markwon**: 4.6.2（Markdown渲染）
- **ML Kit**: 16.0.0（OCR文本识别）

---

## 二、项目目录结构

```
com.example.myapplication/
├── config/                              # 配置层
│   └── ModelConfig.kt                   # AI模型配置
│
├── data/                                # 数据层
│   ├── db/                              # Room数据库
│   │   ├── AppDatabase.kt               # 主数据库
│   │   ├── account/                     # 账户表
│   │   │   ├── AccountDao.kt
│   │   │   ├── AccountDatabase.kt
│   │   │   └── AccountEntity.kt
│   │   └── chat/                        # 聊天表
│   │       ├── ConversationDao.kt       # 对话DAO
│   │       ├── ConversationEntity.kt    # 对话实体
│   │       ├── MessageDao.kt            # 消息DAO
│   │       ├── MessageEntity.kt         # 消息实体
│   │       ├── AttachmentDao.kt         # 附件DAO
│   │       └── AttachmentEntity.kt      # 附件实体
│   ├── model/                           # 数据模型
│   │   └── RecommendedTopic.kt          # 推荐话题
│   └── source/                          # 数据源
│       └── DefaultTopics.kt             # 默认话题数据
│
├── domain/                              # 领域模型层
│   ├── AccountInfo.kt                   # 账户信息
│   ├── ChatHistory.kt                   # 对话历史
│   └── ChatMessage.kt                   # 聊天消息
│
├── network/                             # 网络层
│   ├── ApiClient.kt                     # OkHttp客户端（SSE流式）
│   ├── ApiService.kt                    # 网络服务接口
│   ├── HttpClientProvider.kt            # HTTP客户端提供者
│   ├── WebSearchService.kt              # 联网搜索服务
│   └── model/                           # 网络模型
│       └── ApiModels.kt                 # API请求/响应模型
│
├── ui/                                  # UI层（按功能模块划分）
│   ├── base/                            # 基础类
│   │   └── BaseActivity.kt              # Activity基类
│   │
│   ├── common/                          # 公共组件
│   │   ├── dialogs/                     # 对话框
│   │   │   ├── MessageActionsBottomSheet.kt
│   │   │   └── SelectTextDialogFragment.kt
│   │   ├── managers/                    # 管理器
│   │   │   ├── ModelManager.kt          # 模型管理
│   │   │   └── SidebarManager.kt        # 侧边栏管理
│   │   ├── navigation/                  # 导航
│   │   │   └── AppNavigator.kt          # 应用导航器
│   │   └── views/                       # 自定义视图
│   │       └── TypingIndicatorView.kt   # 打字指示器
│   │
│   ├── auth/                            # 认证模块（MVVM）
│   │   ├── LoginActivity.kt
│   │   ├── AccountViewModel.kt
│   │   └── AccountRepository.kt
│   │
│   ├── main/                            # 主页模块（MVVM）
│   │   ├── MainActivity.kt
│   │   ├── MainViewModel.kt
│   │   └── adapters/
│   │       └── TopicAdapter.kt
│   │
│   ├── chat/                            # 聊天模块（MVVM）
│   │   ├── ChatActivity.kt
│   │   ├── ChatViewModel.kt
│   │   ├── ChatRepository.kt
│   │   └── adapters/
│   │       └── ChatMessageAdapter.kt
│   │
│   ├── history/                         # 历史模块（MVVM + Fragment）⭐
│   │   ├── HistoryFragment.kt           # 可复用Fragment
│   │   ├── HistoryViewModel.kt
│   │   ├── HistoryRepository.kt
│   │   └── adapters/
│   │       └── HistoryAdapter.kt
│   │
│   ├── inputbar/                        # 输入栏模块（MVVM + Fragment）⭐
│   │   ├── InputBarFragment.kt          # 可复用Fragment
│   │   ├── InputBarViewModel.kt
│   │   ├── InputBarRepository.kt
│   │   ├── adapters/
│   │   │   ├── AttachmentPreviewAdapter.kt
│   │   │   └── ModelSelectorAdapter.kt
│   │   └── model/
│   │       └── SelectedMedia.kt
│   │
│   └── search/                          # 搜索模块
│       └── SearchActivity.kt
│
└── utils/                               # 工具类
    ├── MarkwonFactory.kt                # Markwon实例工厂
    ├── WindowInsetsHelper.kt            # WindowInsets适配
    ├── XunfeiSpeechRecognizer.kt        # 语音识别封装
    └── XunfeiSpeechSynthesizer.kt       # 语音合成封装
```

---

## 三、模块详解

### 3.1 Activity模块（4个）

#### 1. LoginActivity（认证模块）

**功能**：
- 用户登录/注册界面
- 表单验证
- 用户认证

**MVVM架构**：
- View: LoginActivity
- ViewModel: AccountViewModel
- Repository: AccountRepository
- Model: AccountEntity

**导航**：
- 登录成功 → MainActivity

---

#### 2. MainActivity（主页模块）

**功能**：
- 推荐话题列表（自动滚动）
- 侧边栏（HistoryFragment）
- 输入栏（InputBarFragment）
- 创建新对话

**MVVM架构**：
- View: MainActivity
- ViewModel: MainViewModel + HistoryViewModel + InputBarViewModel
- Repository: HistoryRepository + InputBarRepository
- Adapter: TopicAdapter

**继承关系**：
- 继承 `BaseHistoryActivity`（提供默认的历史对话处理）

**使用的Fragment**：
- HistoryFragment（侧边栏）
- InputBarFragment（输入栏）

**使用的Manager**：
- SidebarManager（侧边栏管理）

**导航**：
- 点击话题 → ChatActivity
- 发送消息 → ChatActivity

---

#### 3. ChatActivity（聊天模块）

**功能**：
- 聊天消息列表（流式渲染、打字机效果）
- 侧边栏（HistoryFragment）
- 输入栏（InputBarFragment）
- 消息操作（复制、分享、点赞、重新生成）
- 分页加载历史消息

**MVVM架构**：
- View: ChatActivity
- ViewModel: ChatViewModel + HistoryViewModel + InputBarViewModel
- Repository: ChatRepository + HistoryRepository + InputBarRepository
- Adapter: ChatMessageAdapter

**继承关系**：
- 继承 `BaseHistoryActivity`（提供默认的历史对话处理）

**使用的Fragment**：
- HistoryFragment（侧边栏）
- InputBarFragment（输入栏）

**使用的Manager**：
- SidebarManager（侧边栏管理）

**使用的工具**：
- XunfeiSpeechSynthesizer（语音播放）
- MarkwonFactory（Markdown渲染）

**实现的接口**：
- MessageActionsBottomSheet.Listener（消息操作）
- InputBarFragment.InputBarListener（输入事件）

---

#### 4. SearchActivity（搜索模块）

**功能**：
- 搜索历史对话
- 点击搜索结果跳转到ChatActivity

**使用的ViewModel**：
- HistoryViewModel

**导航**：
- 点击结果 → ChatActivity

---

### 3.2 Fragment模块（2个）

#### 1. HistoryFragment（历史对话列表）⭐

**功能**：
- 展示历史对话列表
- 搜索历史对话
- 对话操作：删除、重命名、置顶
- 高亮当前选中的对话

**MVVM架构**：
- View: HistoryFragment
- ViewModel: HistoryViewModel（通过activityViewModels()共享）
- Repository: HistoryRepository
- Adapter: HistoryAdapter

**复用场景**：
- MainActivity的侧边栏
- ChatActivity的侧边栏

**通信方式**：
- 通过 `Listener` 接口回调事件给宿主Activity
- 通过 `activityViewModels()` 共享ViewModel

**接口定义**：
```kotlin
interface Listener {
    fun onHistorySelected(conversationId: String)
    fun onConversationDeleted(conversationId: String)
}
```

---

#### 2. InputBarFragment（输入栏）⭐

**功能**：
- 文本输入
- 语音识别（科大讯飞SDK）
- 附件选择（图片、文件）
- 附件预览
- 联网搜索开关
- 模型选择
- 发送/停止按钮

**MVVM架构**：
- View: InputBarFragment
- ViewModel: InputBarViewModel（通过activityViewModels()共享）
- Repository: InputBarRepository（处理OCR和文件解析）
- Adapter: AttachmentPreviewAdapter, ModelSelectorAdapter

**复用场景**：
- MainActivity的输入栏
- ChatActivity的输入栏

**通信方式**：
- 通过 `InputBarListener` 接口回调事件给宿主Activity
- 通过 `activityViewModels()` 共享ViewModel

**接口定义**：
```kotlin
interface InputBarListener {
    fun onSendClick(text: String, imageUris: List<Uri>, fileUris: List<Uri>)
    fun onStopClick()
    fun onModelSelectorClick()
    fun onWebSearchToggle(isEnabled: Boolean)
    fun onVoiceResult(text: String)
}
```

**使用的工具**：
- XunfeiSpeechRecognizer（语音识别）

---

### 3.3 ViewModel层（5个）

#### 1. AccountViewModel（认证模块）

**职责**：
- 登录表单验证
- 用户认证逻辑
- 登录状态管理

**依赖**：
- AccountRepository

---

#### 2. MainViewModel（主页模块）

**职责**：
- 管理推荐话题列表
- 加载默认话题数据

**依赖**：
- DefaultTopics

---

#### 3. ChatViewModel（聊天模块）

**职责**：
- 管理当前会话的消息列表
- 发送消息并处理流式响应（Flow<Delta>）
- 打字机效果逻辑（批量更新 + 延迟控制）
- 分页加载历史消息
- 停止生成功能
- 联网搜索集成
- 多模态输入处理（OCR、文件解析）
- 消息点赞/点踩持久化

**核心常量**：
- `TYPING_DELAY_MS = 30L`：打字机延迟（约30fps）
- `BATCH_SIZE = 4`：每4字符更新一次UI

**依赖**：
- ChatRepository
- WebSearchService
- InputBarRepository

**LiveData**：
- `messages`: 消息列表
- `messageUpdate`: 消息更新事件
- `isGenerating`: 是否正在生成
- `chatStatus`: 聊天状态（搜索中、搜索完成等）
- `ocrProgress`: OCR进度

---

#### 4. HistoryViewModel（历史记录模块）

**职责**：
- 管理历史对话列表数据
- 创建、重命名、删除、置顶对话
- 搜索历史对话
- 在 MainActivity 和 ChatActivity 之间共享历史数据

**依赖**：
- HistoryRepository

**LiveData**：
- `historyList`: 历史对话列表

**共享方式**：
- 通过 `activityViewModels()` 在多个Activity之间共享

---

#### 5. InputBarViewModel（输入栏模块）

**职责**：
- 管理输入栏的所有状态
- 管理输入模式（键盘/语音）
- 管理附件列表
- 管理输入文本
- 管理联网搜索开关
- 管理生成状态

**LiveData**：
- `isKeyboardMode`: 输入模式
- `attachments`: 附件列表
- `inputText`: 输入文本
- `isWebSearchEnabled`: 联网搜索开关
- `isGenerating`: 生成状态

**共享方式**：
- 通过 `activityViewModels()` 在多个Activity之间共享

---

### 3.4 Repository层（4个）

#### 1. AccountRepository（认证模块）

**职责**：
- 用户账户认证业务逻辑
- 账户数据管理

**依赖**：
- AccountDao

---

#### 2. ChatRepository（聊天模块）

**职责**：
- 对话管理：创建新对话、更新对话标题
- 消息管理：保存消息、查询消息、更新点赞状态、删除消息
- 网络请求：流式对话（委托给ApiClient）

**依赖**：
- MessageDao
- ConversationDao
- ApiClient

---

#### 3. HistoryRepository（历史记录模块）

**职责**：
- 对话历史记录的增删改查
- 创建、删除、更新、置顶对话
- 搜索历史对话

**依赖**：
- ConversationDao

---

#### 4. InputBarRepository（输入栏模块）

**职责**：
- 处理图片OCR识别（ML Kit）
- 处理文件解析（PDF、DOCX、TXT）
- 组合附件内容和用户输入文本

**依赖**：
- MediaUtils

---

### 3.5 基础类模块

#### 1. BaseActivity

**职责**：
- 提供最基础的Activity功能
- WindowInsets处理
- 生命周期管理

---

#### 2. BaseHistoryActivity

**职责**：
- 继承自BaseActivity
- 实现HistoryFragment.Listener接口
- 提供默认的历史对话处理逻辑

**子类**：
- MainActivity
- ChatActivity

**优势**：
- 避免重复代码
- 统一历史对话处理逻辑

---

### 3.6 公共组件模块

#### 1. SidebarManager（侧边栏管理器）

**职责**：
- 管理侧边栏的所有交互逻辑
- 封装侧边栏按钮点击事件
- 管理侧边栏选中状态
- 控制侧边栏打开/关闭

**使用场景**：
- MainActivity
- ChatActivity

---

#### 2. ModelManager（模型管理器）

**职责**：
- 管理AI模型列表
- 显示模型选择对话框
- 持久化模型选择

**使用场景**：
- MainActivity
- ChatActivity

---

#### 3. AppNavigator（应用导航器）

**职责**：
- 统一管理应用内的Activity跳转
- 封装Intent创建和参数传递
- 提供类型安全的导航方法

**核心方法**：
- `navigateToChat()`: 跳转到聊天界面
- `navigateToSearch()`: 跳转到搜索界面
- `navigateToLogin()`: 跳转到登录界面
- `navigateToMain()`: 跳转到主页
- `navigateToHistoryChat()`: 跳转到历史对话
- `navigateToNewChat()`: 创建新对话并跳转

---

#### 4. TypingIndicatorView（打字指示器）

**职责**：
- 自定义视图，显示AI正在输入的动画效果
- 三个圆点依次跳动的动画

**使用场景**：
- ChatMessageAdapter（AI消息项）

---

### 3.7 工具类模块

#### 1. AppPreferences

**职责**：
- 应用偏好设置（SharedPreferences封装）
- 持久化用户配置

---

#### 2. MarkwonFactory

**职责**：
- 创建配置好的Markwon实例
- 统一管理Markdown渲染配置

**使用场景**：
- ChatActivity
- ChatMessageAdapter

---

#### 3. MediaUtils

**职责**：
- 媒体文件处理工具类
- 图片压缩和格式转换
- 文件URI处理

---

#### 4. WindowInsetsHelper

**职责**：
- 提供WindowInsets适配的工具方法
- 统一处理系统栏和键盘的适配

**核心方法**：
- `setupDrawerLayoutInsets()`: DrawerLayout适配（设置系统栏padding）
- `setupInputLayoutInsets()`: 输入布局适配（处理键盘弹出）
- `setupSidebarInsets()`: 侧边栏适配（设置顶部padding）

**使用场景**：
- MainActivity（setupWindowInsetsForDialogue）
- ChatActivity（setupWindowInsetsForChat）

**优势**：
- 统一处理WindowInsets适配
- 避免重复代码
- 易于维护

---

#### 3. XunfeiSpeechRecognizer

**职责**：
- 封装科大讯飞语音识别SDK
- 提供简化的API接口
- 管理识别生命周期

**核心配置**：
- APPID：从BuildConfig读取（安全存储）
- 语言：中文（普通话）
- 前端点超时：4秒
- 后端点超时：1秒
- 自动添加标点符号

**使用场景**：
- InputBarFragment（语音输入）

**优势**：
- 封装第三方SDK，简化使用
- 统一配置管理
- 易于替换SDK

---

#### 4. XunfeiSpeechSynthesizer

**职责**：
- 封装科大讯飞语音合成SDK（TTS）
- 提供文字转语音功能
- 管理合成生命周期

**核心配置**：
- APPID：从BuildConfig读取（安全存储）
- 发音人：可配置（默认小燕）
- 语速、音量、音调：可调节
- 支持在线和离线合成

**使用场景**：
- ChatActivity（播放AI回答）

**优势**：
- 封装第三方SDK，简化使用
- 统一配置管理
- 易于替换SDK

---

## 四、核心架构设计

### 4.1 Fragment组件化设计

**设计理念**：
- 2个核心Fragment（HistoryFragment、InputBarFragment）完全独立
- 通过 `activityViewModels()` 在多个Activity之间共享ViewModel
- 通过 `Listener` 接口回调事件给宿主Activity

**优势**：
- 高复用性：同一个Fragment可在多个Activity中使用
- 状态同步：通过共享ViewModel自动同步状态
- 解耦合：通过接口回调，Fragment不依赖具体的Activity

---

### 4.2 MVVM架构实现

**每个功能模块都遵循完整的MVVM架构**：

| 模块 | Activity/Fragment | ViewModel | Repository | Adapter |
|------|-------------------|-----------|------------|---------|
| auth | LoginActivity | AccountViewModel | AccountRepository | - |
| main | MainActivity | MainViewModel | - | TopicAdapter |
| chat | ChatActivity | ChatViewModel | ChatRepository | ChatMessageAdapter |
| history | HistoryFragment | HistoryViewModel | HistoryRepository | HistoryAdapter |
| inputbar | InputBarFragment | InputBarViewModel | InputBarRepository | AttachmentPreviewAdapter, ModelSelectorAdapter |

---

### 4.3 流式响应架构

**技术实现**：
- **Flow + Coroutines**：处理SSE流式数据
- **打字机效果**：批量更新（每4字符）+ 延迟控制（30ms）
- **局部刷新**：使用PAYLOAD_CONTENT_UPDATE避免整个item重绘

**性能优化**：
- 减少UI渲染压力
- 保持流畅度（约30fps）
- 异步Markdown渲染（包含代码块时）

---

### 4.4 数据库设计（Room v2）

**表结构**：
- **AccountEntity**：账户表
- **ConversationEntity**：对话表（支持软删除、置顶）
- **MessageEntity**：消息表（支持点赞/点踩）
- **AttachmentEntity**：附件表

**特性**：
- 多表关联：Conversation ↔ Message ↔ Attachment
- 分页加载：支持历史消息分页查询
- 索引优化：conversationId、timestamp

---

### 4.5 多模态输入支持

**功能**：
- 图片OCR识别（ML Kit）
- 文件解析（PDF、DOCX、TXT）
- 批量处理：支持多图片/文件同时上传

**实现**：
- InputBarRepository统一处理OCR和文件解析逻辑
- ChatViewModel调用InputBarRepository处理附件

---

## 五、核心业务流程

### 5.1 用户登录流程

```
LoginActivity
  → AccountViewModel.login()
  → AccountRepository.authenticate()
  → AccountDao.query()
  → 验证成功
  → AppNavigator.navigateToMain()
  → MainActivity
```

---

### 5.2 主页发送消息流程

```
MainActivity
  → InputBarFragment（用户输入）
  → InputBarListener.onSendClick()
  → HistoryViewModel.createNewConversation()
  → AppNavigator.navigateToChat()
  → ChatActivity（传递conversationId和initialQuestion）
  → ChatViewModel.sendMessage()
```

---

### 5.3 聊天流式响应流程

```
ChatActivity
  → ChatViewModel.sendMessage()
  → ChatRepository.streamChat()
  → ApiClient.streamChat()
  → OkHttp SSE请求
  → Flow<Delta>流式数据
  → ChatViewModel批量更新（每4字符）
  → MessageUpdateEvent.ItemChanged
  → ChatMessageAdapter.notifyItemChanged()
  → UI更新（打字机效果）
```

---

### 5.4 历史对话切换流程

```
ChatActivity侧边栏
  → HistoryFragment（点击历史对话）
  → HistoryFragment.Listener.onHistorySelected()
  → BaseHistoryActivity默认实现
  → AppNavigator.navigateToHistoryChat()
  → ChatActivity（新的conversationId）
  → ChatViewModel.setConversationId()
  → ChatViewModel.loadInitialMessages()
  → ChatRepository.getLatestMessages()
  → MessageDao.query()
  → UI更新
```

---

### 5.5 多模态输入流程

```
InputBarFragment（选择图片/文件）
  → InputBarListener.onSendClick()
  → ChatViewModel.sendMessageWithAttachments()
  → InputBarRepository.processAttachments()
  → OCR识别（图片）+ 文件解析（PDF/DOCX/TXT）
  → 组合文本内容
  → ChatViewModel.sendMessage()
  → 流式响应流程
```

---

## 六、模块统计

| 模块 | 文件数 | 说明 |
|------|--------|------|
| **config/** | 1 | 模型配置（ModelConfig） |
| **data/db/** | 7 | 数据库（AppDatabase + 3个Account文件 + 6个Chat文件） |
| **data/model/** | 1 | 数据模型（RecommendedTopic） |
| **data/source/** | 1 | 数据源（DefaultTopics） |
| **domain/** | 3 | 领域模型（AccountInfo、ChatHistory、ChatMessage） |
| **network/** | 5 | 网络层（ApiClient、ApiService、HttpClientProvider、WebSearchService + ApiModels） |
| **ui/base/** | 1 | 基础类（BaseActivity、BaseHistoryActivity） |
| **ui/common/** | 7 | 公共组件（2个Dialog + 2个Manager + 1个Navigation + 1个View + 1个Model） |
| **ui/auth/** | 3 | 认证模块（LoginActivity + AccountViewModel + AccountRepository） |
| **ui/main/** | 3 | 主页模块（MainActivity + MainViewModel + TopicAdapter） |
| **ui/chat/** | 4 | 聊天模块（ChatActivity + ChatViewModel + ChatRepository + ChatMessageAdapter） |
| **ui/history/** | 4 | 历史模块（HistoryFragment + HistoryViewModel + HistoryRepository + HistoryAdapter） |
| **ui/inputbar/** | 7 | 输入栏模块（InputBarFragment + InputBarViewModel + InputBarRepository + 2个Adapter + 2个Model） |
| **ui/search/** | 1 | 搜索模块（SearchActivity） |
| **utils/** | 4 | 工具类（MarkwonFactory、WindowInsetsHelper、2个讯飞SDK封装） |
| **总计** | **53** | 核心代码文件 |

### 详细文件清单

#### config/ (1个文件)
- ModelConfig.kt

#### data/ (9个文件)
- **db/** (7个文件)
  - AppDatabase.kt
  - account/AccountDao.kt
  - account/AccountDatabase.kt
  - account/AccountEntity.kt
  - chat/ConversationDao.kt
  - chat/ConversationEntity.kt
  - chat/MessageDao.kt
  - chat/MessageEntity.kt
  - chat/AttachmentDao.kt
  - chat/AttachmentEntity.kt
- **model/** (1个文件)
  - RecommendedTopic.kt
- **source/** (1个文件)
  - DefaultTopics.kt

#### domain/ (3个文件)
- AccountInfo.kt
- ChatHistory.kt
- ChatMessage.kt

#### network/ (5个文件)
- ApiClient.kt
- ApiService.kt
- HttpClientProvider.kt
- WebSearchService.kt
- model/ApiModels.kt

#### ui/ (33个文件)
- **base/** (1个文件)
  - BaseActivity.kt
- **common/** (7个文件)
  - dialogs/MessageActionsBottomSheet.kt
  - dialogs/SelectTextDialogFragment.kt
  - managers/ModelManager.kt
  - managers/SidebarManager.kt
  - navigation/AppNavigator.kt
  - views/TypingIndicatorView.kt
- **auth/** (3个文件)
  - LoginActivity.kt
  - AccountViewModel.kt
  - AccountRepository.kt
- **main/** (3个文件)
  - MainActivity.kt
  - MainViewModel.kt
  - adapters/TopicAdapter.kt
- **chat/** (4个文件)
  - ChatActivity.kt
  - ChatViewModel.kt
  - ChatRepository.kt
  - adapters/ChatMessageAdapter.kt
- **history/** (4个文件)
  - HistoryFragment.kt
  - HistoryViewModel.kt
  - HistoryRepository.kt
  - adapters/HistoryAdapter.kt
- **inputbar/** (7个文件)
  - InputBarFragment.kt
  - InputBarViewModel.kt
  - InputBarRepository.kt
  - adapters/AttachmentPreviewAdapter.kt
  - adapters/ModelSelectorAdapter.kt
  - model/SelectedMedia.kt
- **search/** (1个文件)
  - SearchActivity.kt

#### utils/ (4个文件)
- MarkwonFactory.kt
- WindowInsetsHelper.kt
- XunfeiSpeechRecognizer.kt
- XunfeiSpeechSynthesizer.kt

---

## 七、架构优势

### 7.1 Fragment组件化

- **高复用性**：2个Fragment在多个Activity中复用
- **状态同步**：通过共享ViewModel自动同步状态
- **解耦合**：通过接口回调，Fragment不依赖具体的Activity
- **独立完整**：每个Fragment都有完整的MVVM结构

### 7.2 MVVM架构

- **职责清晰**：View、ViewModel、Repository各司其职
- **易于测试**：ViewModel和Repository可独立测试
- **数据驱动**：LiveData自动更新UI
- **完整实现**：5个ViewModel + 4个Repository

### 7.3 模块化设计

- **功能内聚**：每个模块包含完整的MVVM结构
- **易于维护**：修改功能时所有相关文件都在同一目录
- **易于扩展**：新增功能只需添加新模块
- **清晰分层**：config、data、domain、network、ui、utils

### 7.4 Manager管理器模式

- **职责单一**：每个Manager只负责一个功能
- **易于测试**：Manager可独立测试
- **高复用性**：Manager可在多个Activity中复用
- **组合优于继承**：通过组合方式使用功能

### 7.5 代码质量

- **无重复代码**：已删除冗余的AppPreferences
- **统一管理**：ModelManager统一管理模型配置
- **工具类精简**：只保留必要的4个工具类
- **架构清晰**：53个核心文件，结构清晰

---

## 八、技术亮点

### 8.1 流式响应

- SSE流式数据处理
- 打字机效果（批量更新 + 延迟控制）
- 局部刷新优化

### 8.2 多模态输入

- 图片OCR识别（ML Kit）
- 文件解析（PDF、DOCX、TXT）
- 批量处理

### 8.3 性能优化

- RecyclerView长列表优化
- 分页加载历史消息
- 异步Markdown渲染
- 协程管理和取消

### 8.4 用户体验

- 打字机效果
- 语音识别和合成
- 联网搜索增强
- 消息操作（复制、分享、点赞、重新生成）

---

## 九、项目总结

### 9.1 架构亮点 ✨

1. **清晰的模块划分**
   - 3个核心Activity + 2个可复用Fragment
   - 每个模块都有完整的MVVM结构
   - 53个核心文件，结构清晰

2. **Fragment组件化**
   - HistoryFragment和InputBarFragment在多个Activity中复用
   - 通过activityViewModels()共享状态
   - 通过Listener接口解耦

3. **无重复代码**
   - 已删除冗余的AppPreferences
   - BaseHistoryActivity统一处理历史对话逻辑
   - Manager模式统一管理公共功能

4. **完整的MVVM架构**
   - 5个ViewModel + 4个Repository
   - LiveData驱动UI更新
   - 职责清晰，易于测试

5. **流式响应优化**
   - SSE流式数据处理
   - 打字机效果（批量更新 + 延迟控制）
   - 局部刷新优化

### 9.2 技术特色 🚀

1. **多模态输入**
   - 图片OCR识别（ML Kit）
   - 文件解析（PDF、DOCX、TXT）
   - 语音识别和合成（科大讯飞SDK）

2. **性能优化**
   - RecyclerView长列表优化
   - 分页加载历史消息
   - 异步Markdown渲染
   - 协程管理和取消

3. **用户体验**
   - 打字机效果
   - 联网搜索增强
   - 消息操作（复制、分享、点赞、重新生成）
   - 语音播放

### 9.3 代码质量 ✅

- **无重复代码**：已删除冗余文件
- **架构清晰**：分层明确，职责单一
- **易于维护**：模块化设计，功能内聚
- **易于扩展**：新增功能只需添加新模块

---

## 十、待优化项

### 10.1 功能完善

- [ ] 断网重连自动同步
- [ ] 消息发送失败重试
- [ ] 图片压缩优化
- [ ] 语音识别准确率提升

### 10.2 性能优化

- [ ] 数据库查询优化
- [ ] 内存泄漏检测
- [ ] 启动速度优化
- [ ] 包体积优化

### 10.3 代码质量

- [ ] 单元测试覆盖
- [ ] UI测试覆盖
- [ ] 代码规范检查
- [ ] 文档完善

---

**文档结束**

> 本文档详细描述了项目的架构设计、模块划分、技术实现和优化方向。  
> 项目采用MVVM架构，3个Activity + 2个Fragment的设计，代码结构清晰，无重复代码。  
> 总计53个核心代码文件，实现了完整的AI聊天应用功能。
