# 项目架构文档

## 一、架构概述

### 1. 架构模式

- **MVVM 架构**（Model-View-ViewModel）
- **模块化设计**：采用 Feature + Fragment 组件化架构
- **数据层**：本地数据库（Room）+ 网络请求（OkHttp + SSE流式响应）
- **UI 层**：Activity + Fragment + RecyclerView + ViewBinding
- **响应式编程**：LiveData + Kotlin Coroutines + Flow

### 2. 核心功能模块

- **用户认证**：登录/注册（AccountViewModel + AccountRepository）
- **对话会话管理**：创建、历史记录、删除、置顶、搜索（HistoryViewModel）
- **实时消息展示**：流式响应、打字机效果、Markdown渲染（ChatViewModel）
- **多模型切换**：支持豆包、DeepSeek、GPT等多种AI模型
- **联网搜索增强**：集成Serper搜索API，增强AI回答准确性
- **多模态输入**：文本、图片OCR、文件解析（PDF/DOCX/TXT）
- **语音识别**：科大讯飞SDK + Google语音识别双引擎
- **数据持久化**：Room数据库（多表关联）
- **性能优化**：长列表优化、分页加载、假数据生成测试

---

## 二、项目目录结构（实际架构）

```
com.example.myapplication/
├── ui/                                  # UI层 - Activity/Fragment界面
│   ├── LoginActivity.kt                 # 登录界面
│   ├── DialogueActivity.kt              # 主页（推荐话题 + 历史对话侧边栏）
│   ├── ChatActivity.kt                  # 聊天对话界面
│   ├── SearchActivity.kt                # 搜索界面
│   ├── BaseAttachmentActivity.kt        # 基类：附件处理、语音识别、联网搜索
│   ├── HistoryFragment.kt               # 历史对话列表Fragment
│   ├── InputBoxFragment.kt              # 输入框Fragment（键盘/语音模式切换）
│   ├── WebSearchAndModelFragment.kt     # 联网搜索和模型选择Fragment
│   ├── SelectTextDialogFragment.kt      # 文本选择弹窗
│   └── MessageActionsBottomSheet.kt     # 聊天消息操作菜单（长按弹出）
│
├── feature_chat/                        # 聊天功能模块（预留）
│   └── view/                            # 聊天相关视图组件
│
├── feature_history/                     # 历史记录功能模块（预留）
│
├── feature_login/                       # 登录功能模块（预留）
│   └── view/                            # 登录相关视图组件
│
├── feature_search/                      # 搜索功能模块（预留）
│   └── view/                            # 搜索相关视图组件
│
├── viewmodel/                           # ViewModel层 - 业务逻辑
│   ├── AccountViewModel.kt              # 账户登录/注册业务逻辑
│   ├── DialogueViewModel.kt             # 主页业务逻辑（推荐话题）
│   ├── HistoryViewModel.kt              # 历史对话列表业务逻辑
│   └── ChatViewModel.kt                 # 聊天业务逻辑（核心）
│
├── repository/                          # Repository层 - 数据仓库
│   ├── AccountRepository.kt             # 账户数据仓库
│   ├── UserRepository.kt                # 用户数据仓库
│   ├── HistoryRepository.kt             # 历史记录仓库
│   └── ChatRepository.kt                # 聊天数据仓库（核心）
│
├── adapter/                             # Adapter - RecyclerView适配器
│   ├── HistoryAdapter.kt                # 历史对话列表适配器
│   ├── ChatMessageAdapter.kt            # 聊天消息适配器（支持Markdown渲染）
│   ├── ModelAdapter.kt                  # AI模型选择适配器
│   ├── TopicAdapter.kt                  # 推荐话题适配器
│   └── PreviewAdapter.kt                # 图片/文件预览适配器
│
├── data/                                # 数据层
│   ├── db/                              # Room数据库相关
│   │   ├── AppDatabase.kt               # 主数据库实例（v11）
│   │   ├── AccountDao.kt                # 账户DAO
│   │   ├── AccountEntity.kt             # 账户实体
│   │   ├── AccountDatabase.kt           # 账户数据库配置
│   │   ├── ConversationDao.kt           # 对话DAO
│   │   ├── ConversationEntity.kt        # 对话实体
│   │   ├── MessageDao.kt                # 消息DAO
│   │   ├── MessageEntity.kt             # 消息实体
│   │   ├── UserDao.kt                   # 用户DAO
│   │   ├── UserEntity.kt                # 用户实体
│   │   ├── AttachmentDao.kt             # 附件DAO
│   │   └── AttachmentEntity.kt          # 附件实体
│   └── DefaultTopics.kt                 # 默认推荐话题数据
│
├── model/                               # 数据模型
│   ├── AccountInfo.kt                   # 账户UI模型
│   ├── ChatMessage.kt                   # 聊天消息模型
│   ├── ChatHistory.kt                   # 对话历史模型
│   ├── ChatModels.kt                    # AI模型注册表
│   ├── ModelConfig.kt                   # 模型配置（参数、温度等）
│   ├── SelectedMedia.kt                 # 附件预览模型
│   ├── ApiMessage.kt                    # API请求消息格式
│   └── Delta.kt                         # SSE流式响应增量数据
│
├── network/                             # 网络层
│   ├── ApiService.kt                    # 网络服务接口（Flow流式）
│   ├── ApiClient.kt                     # OkHttp客户端配置（SSE支持）
│   ├── WebSearchService.kt              # 联网搜索接口
│   └── SerperWebSearchService.kt        # Serper搜索API实现
│
├── utils/                               # 工具类
│   ├── ModelPreferences.kt              # 模型选择持久化
│   ├── DialogHelper.kt                  # 对话框辅助类
│   ├── OCRHelper.kt                     # OCR文本识别（ML Kit）
│   └── XunfeiSpeechRecognizer.kt        # 科大讯飞语音识别封装
│
└── res/layout/                          # 布局文件
    ├── activity_login.xml               # 登录页面
    ├── activity_dialogue.xml            # 主页布局
    ├── activity_chat.xml                # 聊天界面
    ├── activity_search.xml              # 搜索界面
    ├── fragment_history.xml             # 历史列表Fragment
    ├── fragment_input_box.xml           # 输入框Fragment
    ├── fragment_web_search_and_model.xml # 搜索和模型Fragment
    ├── include_sidebar_common.xml       # 侧边栏公共布局
    ├── dialog_custom_input.xml          # 自定义输入对话框
    ├── dialog_select_text.xml           # 文本选择对话框
    ├── dialog_attachment_options.xml    # 附件选项对话框
    ├── dialog_model_selector.xml        # 模型选择对话框
    ├── bottom_sheet_message_actions.xml # 消息操作底部菜单
    ├── item_history.xml                 # 历史对话列表项
    ├── item_message_user.xml            # 用户消息列表项
    ├── item_message_ai.xml              # AI消息列表项
    ├── item_model_option.xml            # 模型选项列表项
    ├── item_topic.xml                   # 推荐话题列表项
    ├── item_media_preview.xml           # 附件预览列表项
    └── item_dialog_menu.xml             # 菜单列表项
```

---

## 三、核心架构设计

### 🎯 架构特点

#### 1. **Fragment组件化设计**
- **InputBoxFragment**：独立管理输入框、附件预览、语音/键盘模式切换
- **HistoryFragment**：独立管理历史对话列表、搜索、操作菜单
- **WebSearchAndModelFragment**：独立管理联网搜索开关和模型选择
- **优势**：解耦UI组件，提高复用性和可维护性

#### 2. **BaseAttachmentActivity基类**
- 封装通用功能：附件选择、语音识别、联网搜索
- 子类（ChatActivity、DialogueActivity）继承复用
- 统一权限管理和生命周期处理

#### 3. **流式响应架构**
- **Flow + Coroutines**：处理SSE流式数据
- **打字机效果**：批量更新（每4字符）+ 延迟控制（30ms）
- **性能优化**：减少UI渲染压力，保持流畅度

#### 4. **数据库设计（Room v11）**
- **多表关联**：Conversation ↔ Message ↔ Attachment
- **分页加载**：支持历史消息分页查询
- **点赞状态持久化**：isLiked、isDisliked字段

#### 5. **多模态输入支持**
- **图片OCR**：ML Kit文本识别（中英文）
- **文件解析**：PDF（PdfRenderer + OCR）、DOCX（ZIP解析）、TXT
- **批量处理**：支持多图片/文件同时上传

---

## 四、文件功能详解及联动关系

### 📱 UI层 (Activity & Fragment)

#### 1. **LoginActivity.kt**

**功能**：
- 应用启动入口和登录界面
- 用户登录/注册功能
- 用户认证成功后跳转到DialogueActivity

**联动关系**：
- 使用 `AccountViewModel` 处理登录业务逻辑
- 通过 `AccountRepository` 验证用户凭证
- 登录成功跳转 → `DialogueActivity`

---

#### 2. **DialogueActivity.kt**

**功能**：

- 应用主页界面
- 显示推荐话题列表（可点击快速开始对话）
- 侧边栏显示历史对话列表
- 支持语音模式/键盘模式切换
- 支持联网搜索开关
- 模型选择对话框

**联动关系**：

- 使用 `DialogueViewModel` 管理推荐话题和语音模式
- 使用 `HistoryViewModel` 管理历史对话列表（共享数据）
- 通过 `TopicAdapter` 展示推荐话题
- 通过 `HistoryAdapter` 展示历史对话列表
- 使用 `ModelAdapter` 在对话框中展示可选模型
- 点击话题或历史对话 → `ChatActivity`（传递conversationId和初始问题）
- 点击创建新对话 → 创建新conversationId并清空输入

---

#### 3. **ChatActivity.kt**（核心聊天界面）

**功能**：
- 聊天对话界面（继承自BaseAttachmentActivity）
- 显示用户和AI的消息记录（支持Markdown渲染）
- 发送消息给AI并接收流式响应
- 打字机效果展示AI回复（批量更新优化）
- 分页加载历史消息（下拉刷新）
- 消息操作：复制、分享、点赞/点踩、重新生成
- 集成3个Fragment：InputBoxFragment、HistoryFragment、WebSearchAndModelFragment

**联动关系**：
- 使用 `ChatViewModel` 管理消息数据和发送逻辑
- 使用 `HistoryViewModel` 管理侧边栏历史列表
- 使用 `DialogueViewModel` 共享语音模式状态
- 通过 `ChatRepository` 发送网络请求和保存消息
- 使用 `ChatMessageAdapter` 展示消息列表（注入Markwon实例）
- 接收 `DialogueActivity` 传递的 `conversationId` 和初始问题
- 自动加载该会话的历史消息
- 发送消息后更新 `ConversationEntity` 的最后更新时间
- 实现 `MessageActionsBottomSheet.Listener` 处理消息操作
- 实现 `HistoryFragment.Listener` 处理历史列表点击
- 实现 `InputBoxFragment.InputBoxListener` 处理输入事件
- 实现 `WebSearchAndModelFragment.WebSearchAndModelListener` 处理搜索和模型切换

#### 4. **BaseAttachmentActivity.kt**（抽象基类）

**功能**：
- 封装附件选择逻辑（图片、文件）
- 封装语音识别逻辑（科大讯飞 + Google双引擎）
- 封装联网搜索开关管理
- 统一权限请求处理
- 提供抽象方法供子类实现

**核心方法**：
- `selectImages()` / `selectFiles()`：选择附件
- `startVoiceRecognition()` / `stopVoiceRecognition()`：语音识别
- `setNetworkSearchEnabled()`：联网搜索开关
- `onVoiceRecognitionResult()`：语音识别结果回调（抽象）
- `getHoldToSpeakView()`：获取语音按钮视图（抽象）

**联动关系**：
- 被 `ChatActivity` 和 `DialogueActivity` 继承
- 使用 `XunfeiSpeechRecognizer` 进行语音识别
- 使用 `PreviewAdapter` 展示附件预览
- 通过 `SelectedMedia` 模型管理附件列表

#### 5. **HistoryFragment.kt**

**功能**：
- 独立管理历史对话列表
- 支持对话操作：重命名、删除、置顶
- 支持搜索历史对话
- 高亮当前选中的对话

**联动关系**：
- 使用 `HistoryViewModel` 获取历史数据
- 使用 `HistoryAdapter` 展示列表
- 通过 `Listener` 接口回调点击事件给Activity
- 在 `ChatActivity` 和 `DialogueActivity` 中复用

#### 6. **InputBoxFragment.kt**

**功能**：
- 独立管理输入框UI
- 键盘/语音模式切换
- 附件预览列表管理
- 发送/停止按钮控制

**联动关系**：
- 通过 `InputBoxListener` 接口回调事件给Activity
- 使用 `PreviewAdapter` 展示附件预览
- 接收Activity的UI更新指令（updateVoiceModeUI等）

#### 7. **WebSearchAndModelFragment.kt**

**功能**：
- 独立管理联网搜索开关
- 独立管理AI模型选择

**联动关系**：
- 通过 `WebSearchAndModelListener` 接口回调事件给Activity
- 使用 `ModelPreferences` 持久化模型选择
- 使用 `ModelAdapter` 展示模型列表

---

### 🧠 ViewModel层

#### 8. **AccountViewModel.kt**

**功能**：
- 登录表单验证
- 用户认证逻辑
- 登录状态管理

**联动关系**：
- 依赖 `AccountRepository` 进行用户认证
- 通过 LiveData 向 `LoginActivity` 发送登录结果

---

#### 5. **DialogueViewModel.kt**

**功能**：

- 管理主页特有功能（推荐话题、语音模式）
- 加载和初始化推荐话题列表
- 管理语音模式/键盘模式状态
- 管理Toast消息和输入框清空状态

**联动关系**：

- 依赖 `RecommendedTopicDao` 获取推荐话题
- 通过 LiveData 向 `DialogueActivity` 推送话题列表和状态更新
- 初始化时插入默认话题（如果数据库为空）
- **不再管理历史对话列表**（已拆分到 `HistoryViewModel`）

---

#### 6. **HistoryViewModel.kt**（新增）

**功能**：

- 独立管理历史对话列表数据
- 创建、重命名、删除、置顶对话
- 搜索历史对话
- 在 `DialogueActivity` 和 `ChatActivity` 之间共享历史数据

**联动关系**：

- 依赖 `HistoryRepository` 和 `ChatRepository` 获取和操作对话数据
- 通过 LiveData 向两个 Activity 推送历史列表更新
- 创建新对话时生成新的 `ConversationEntity` 并返回 conversationId
- 支持按标题搜索对话
- **职责分离**：从 `DialogueViewModel` 中拆分出来，专注于历史管理

---

#### 11. **ChatViewModel.kt**（核心业务逻辑）

**功能**：
- 管理当前会话的消息列表（MutableList<ChatMessage>）
- 发送消息并处理流式响应（Flow<Delta>）
- 打字机效果逻辑（批量更新 + 延迟控制）
- 分页加载历史消息（loadMoreHistory）
- 管理conversationId（自动创建新对话）
- 停止生成功能（stopGeneration）
- 联网搜索集成（WebSearchService）
- 多模态输入处理：
  - 图片OCR识别（batchOCRImages）
  - 文件解析（PDF/DOCX/TXT）
  - 附件内容拼接
- 模型切换管理（switchModel）
- 消息点赞/点踩持久化
- 假数据生成（debugSeedFakeConversation，用于测试长列表）

**核心常量**：
- `TYPING_DELAY_MS = 30L`：打字机延迟（约30fps）
- `BATCH_SIZE = 4`：每4字符更新一次UI
- `MAX_FILE_PREVIEW_CHARS = 8000`：文件预览最大字符数

**联动关系**：
- 依赖 `ChatRepository` 发送消息和保存数据
- 依赖 `SerperWebSearchService` 进行联网搜索
- 依赖 `OCRHelper` 进行图片文字识别
- 通过 LiveData 向 `ChatActivity` 推送消息更新
- 使用 `MessageUpdateEvent` 精确通知UI更新类型：
  - `ItemInserted`：新消息插入
  - `ItemChanged`：消息内容更新（流式输出）
  - `HistoryLoaded`：历史消息加载完成
  - `NoMoreHistory`：没有更多历史
- 在发送消息时关联 `conversationId`
- 调用 `loadInitialMessages()` 从数据库加载历史消息
- 使用 `ModelPreferences` 持久化模型选择

---

### 💾 Repository层 (数据仓库)

#### 12. **AccountRepository.kt**

**功能**：
- 用户账户认证业务逻辑
- 账户数据管理
- 与AccountDatabase交互

**联动关系**：
- 被 `AccountViewModel` 调用
- 依赖 `AccountDao` 访问数据库

#### 13. **UserRepository.kt**

**功能**：
- 用户数据管理
- 用户信息持久化

**联动关系**：
- 依赖 `UserDao` 访问数据库
- 被相关ViewModel调用

#### 14. **HistoryRepository.kt**

**功能**：
- 对话历史记录的增删改查
- 从Room数据库获取对话列表
- 创建、删除、更新、置顶对话会话
- 搜索历史对话

**联动关系**：
- 依赖 `ConversationDao` 访问数据库
- 被 `HistoryViewModel` 调用
- 返回 `List<ChatHistory>` 数据模型

#### 15. **ChatRepository.kt**（核心数据仓库）

**功能**：
- **对话管理**：
  - 创建新对话（createConversation）
  - 更新对话标题（updateConversationTitle）
  - 自动更新对话时间戳和消息计数
- **消息管理**：
  - 保存消息到数据库（saveMessage）
  - 查询最新消息（getLatestMessages）
  - 分页查询历史消息（getMessagesBefore）
  - 更新消息点赞状态（updateMessageLikeState）
  - 删除消息（deleteMessagesByTimestamps）
- **网络请求**：
  - 流式对话（streamChat）
  - 委托给 `ApiClient` 单例，复用连接池

**联动关系**：
- 依赖 `MessageDao` 和 `ConversationDao` 访问数据库
- 依赖 `ApiClient` 发送网络请求
- 被 `ChatViewModel` 和 `HistoryViewModel` 调用
- 将 `MessageEntity` 转换为 `ChatMessage` 模型
- 将 `ChatMessage` 转换为 `MessageEntity` 持久化

---

### 🎨 Adapter层

#### 16. **HistoryAdapter.kt**

**功能**：
- RecyclerView适配器，展示对话历史列表
- 绑定 `item_history.xml` 布局
- 处理列表项点击事件
- 支持高亮当前选中对话

**联动关系**：
- 被 `HistoryFragment` 使用
- 数据源来自 `HistoryViewModel`
- 点击事件通过Fragment回调给Activity

#### 17. **ChatMessageAdapter.kt**（核心适配器）

**功能**：
- RecyclerView适配器，展示聊天消息
- 区分用户消息和AI消息（不同ViewType）
- 支持打字机效果更新（局部刷新）
- 集成Markdown渲染（Markwon）
- 支持代码高亮、表格、任务列表等
- 消息操作按钮：分享、点赞、点踩、重新生成
- 长按弹出操作菜单
- 性能优化：
  - 使用 `PAYLOAD_CONTENT_UPDATE` 局部刷新
  - 缓存已播放动画的消息（markMessagesAsAnimated）
  - 设置 `setHasFixedSize(true)` 和 `setItemViewCacheSize(20)`

**联动关系**：
- 被 `ChatActivity` 使用
- 数据源来自 `ChatViewModel` 的消息列表
- 注入 `Markwon` 实例进行渲染
- 实时更新AI消息内容（流式响应）
- 长按回调 `onAiMessageLongClick` 弹出操作菜单
- 按钮点击回调：onShareClick、onLikeClick、onDislikeClick、onReloadClick

#### 18. **ModelAdapter.kt**

**功能**：
- RecyclerView适配器，展示可选AI模型
- 绑定 `item_model_option.xml` 布局
- 处理模型选择事件
- 高亮当前选中模型

**联动关系**：
- 被 `WebSearchAndModelFragment` 使用
- 数据源来自 `ModelRegistry.ALL_MODELS`
- 选择结果通过Fragment回调给Activity
- Activity调用 `ChatViewModel.switchModel()` 切换模型

#### 19. **TopicAdapter.kt**

**功能**：
- RecyclerView适配器，展示推荐话题
- 绑定 `item_topic.xml` 布局
- 处理话题点击事件

**联动关系**：
- 被 `DialogueActivity` 使用
- 数据源来自 `DialogueViewModel` 的话题列表
- 点击话题后创建新对话并跳转到 `ChatActivity`

#### 20. **PreviewAdapter.kt**

**功能**：
- RecyclerView适配器，展示附件预览
- 支持图片和文件预览
- 支持删除附件

**联动关系**：
- 被 `InputBoxFragment` 使用
- 数据源来自 `BaseAttachmentActivity` 的 `selectedMediaList`
- 删除回调 `onDeleteClick` 移除附件

---

### 🗄️ 数据库层

#### 21. **AppDatabase.kt**（主数据库）

**功能**：
- Room数据库实例（单例）
- 定义数据库版本和迁移策略
- 提供DAO访问接口

**数据库配置**：
- 版本：v11
- 迁移策略：`fallbackToDestructiveMigration()`（升级时清空数据）
- 包含实体：MessageEntity、ConversationEntity、UserEntity、AttachmentEntity

**联动关系**：
- 提供 `messageDao()`、`conversationDao()`、`userDao()`、`attachmentDao()`
- 被所有Repository调用获取DAO实例
- 使用单例模式确保全局唯一实例

---

#### 22. **MessageEntity.kt**

**功能**：
- 消息表的实体类
- 字段：
  - `id`：主键（自增）
  - `conversationId`：所属对话ID
  - `content`：消息内容
  - `isUser`：是否为用户消息
  - `timestamp`：时间戳
  - `reasoningContent`：推理内容（DeepSeek等模型）
  - `isComplete`：是否完成（流式输出标记）
  - `isLiked`：是否点赞
  - `isDisliked`：是否点踩

**联动关系**：
- 被 `MessageDao` 使用
- 与 `ConversationEntity` 通过 `conversationId` 关联（外键）
- 被 `ChatRepository` 保存到数据库
- 转换为 `ChatMessage` 模型用于UI展示

#### 23. **MessageDao.kt**

**功能**：
- 消息表的数据访问对象（DAO）
- 提供方法：
  - `insertMessage()`：插入消息
  - `getLatestMessages()`：查询最新N条消息
  - `getMessagesBefore()`：分页查询历史消息
  - `updateMessageLikeState()`：更新点赞状态
  - `deleteMessagesByTimestamps()`：批量删除消息

**联动关系**：
- 被 `ChatRepository` 调用
- 操作 `MessageEntity`
- 支持分页查询和条件查询

#### 24. **ConversationEntity.kt**

**功能**：
- 对话会话表的实体类
- 字段：
  - `id`：对话ID（UUID）
  - `title`：对话标题
  - `createdAt`：创建时间
  - `updatedAt`：最后更新时间
  - `messageCount`：消息数量
  - `lastMessagePreview`：最后一条消息预览
  - `isPinned`：是否置顶

**联动关系**：
- 被 `ConversationDao` 使用
- 与 `MessageEntity` 是一对多关系
- 被 `HistoryRepository` 和 `ChatRepository` 操作
- 转换为 `ChatHistory` 模型用于UI展示

#### 25. **ConversationDao.kt**

**功能**：
- 对话会话表的数据访问对象（DAO）
- 提供方法：
  - `insertConversation()`：创建对话
  - `getConversationById()`：根据ID查询
  - `getRecentConversations()`：查询最近对话（置顶优先）
  - `updateConversation()`：更新对话
  - `deleteConversation()`：删除对话
  - `searchConversations()`：搜索对话

**联动关系**：
- 被 `HistoryRepository` 和 `ChatRepository` 调用
- 操作 `ConversationEntity`
- 支持置顶排序和搜索

#### 26. **UserEntity.kt & UserDao.kt**

**功能**：
- 用户表的实体类和DAO
- 存储用户基本信息

**联动关系**：
- 被 `UserRepository` 使用
- 用于用户数据持久化

#### 27. **AccountEntity.kt & AccountDao.kt**

**功能**：
- 账户表的实体类和DAO
- 存储账户认证信息

**联动关系**：
- 被 `AccountRepository` 使用
- 用于账户认证和管理

#### 28. **AttachmentEntity.kt & AttachmentDao.kt**

**功能**：
- 附件表的实体类和DAO
- 字段：id, messageId, filePath, fileType, fileSize
- 用于存储消息的附件（图片、文件等）

**联动关系**：
- 与 `MessageEntity` 通过 messageId 关联
- 为未来的多模态功能预留
- 支持图片上传、文件附件等功能

---

### 🌐 网络层

#### 29. **ApiService.kt**（接口定义）

**功能**：
- 网络服务接口定义
- 方法：`streamChat(messages, modelConfig): Flow<Delta>`
- 支持流式响应（SSE）

**联动关系**：
- 被 `ApiClient` 实现
- 被 `ChatRepository` 调用发送消息
- 接收 `List<ApiMessage>` 和 `ModelConfig`
- 返回 `Flow<Delta>` 流式数据

#### 30. **ApiClient.kt**（核心网络客户端）

**功能**：
- OkHttpClient配置（单例）
- SSE流式响应解析
- 设置超时、拦截器、日志
- 实现 `ApiService` 接口

**核心配置**：
- 连接超时：30秒
- 读取超时：60秒
- 写入超时：60秒
- 支持HTTP/2
- 连接池复用

**SSE解析逻辑**：
- 逐行读取响应流
- 解析 `data:` 开头的行
- 提取 `delta.content` 和 `delta.reasoning_content`
- 转换为 `Delta` 对象
- 通过 `Flow` 发射给上层

**联动关系**：
- 提供 `streamChat()` 方法
- 被 `ChatRepository` 调用
- 使用 `callbackFlow` 和 `awaitClose` 管理协程生命周期

#### 31. **WebSearchService.kt**（接口定义）

**功能**：
- 联网搜索服务接口
- 方法：`search(query): String`

**联动关系**：
- 被 `SerperWebSearchService` 实现
- 被 `ChatViewModel` 调用

#### 32. **SerperWebSearchService.kt**（实现类）

**功能**：
- 集成Serper搜索API
- 解析搜索结果
- 格式化为Markdown文本

**搜索结果格式**：
```
1. [标题](链接)
   摘要内容
```

**联动关系**：
- 实现 `WebSearchService` 接口
- 被 `ChatViewModel` 使用
- 当用户开启联网搜索时，先搜索再发送给AI
- 搜索结果作为上下文传递给AI模型

---

### 📦 Model层

#### 33. **ChatMessage.kt**（UI消息模型）

**功能**：
- 聊天消息的UI数据模型
- 字段：
  - `content`：消息内容
  - `isUser`：是否为用户消息
  - `timestamp`：时间戳（默认当前时间）
  - `reasoningContent`：推理内容（可选）
  - `isComplete`：是否完成（流式输出标记）
  - `isLiked`：是否点赞
  - `isDisliked`：是否点踩

**联动关系**：
- 被 `ChatViewModel` 和 `ChatMessageAdapter` 使用
- 从 `MessageEntity` 转换而来
- 用于UI展示和内存操作

#### 34. **ChatHistory.kt**

**功能**：
- 对话历史的UI数据模型
- 包含对话标题、最后消息时间、消息预览

**联动关系**：
- 被 `HistoryViewModel` 和 `HistoryAdapter` 使用
- 从 `ConversationEntity` 转换而来
- 用于历史列表展示

#### 35. **ChatModels.kt**（模型注册表）

**功能**：
- 定义可用的AI模型列表
- `ModelRegistry.ALL_MODELS`：所有可用模型
- `ModelRegistry.DEFAULT_MODEL`：默认模型

**支持的模型**：
- DeepSeek系列（deepseek-chat、deepseek-reasoner）
- 豆包系列（doubao-pro、doubao-lite）
- GPT系列（gpt-4o、gpt-4o-mini）
- Claude系列（claude-3.5-sonnet）
- Gemini系列（gemini-2.0-flash）

**联动关系**：
- 被 `ModelAdapter` 使用
- 被 `ModelPreferences` 使用
- 用户选择的模型保存到 SharedPreferences

#### 36. **ModelConfig.kt**（模型配置）

**功能**：
- AI模型配置类
- 字段：
  - `id`：模型唯一标识
  - `displayName`：显示名称
  - `apiModel`：API模型名称
  - `maxTokens`：最大token数
  - `temperature`：温度参数
  - `enableThinking`：是否启用推理模式

**联动关系**：
- 被 `ChatRepository` 使用构造请求参数
- 被 `ChatViewModel` 管理当前模型
- 与 `ChatModels.kt` 配合使用

#### 37. **ApiMessage.kt**

**功能**：
- API请求消息格式
- 字段：`role`（user/assistant）、`content`

**联动关系**：
- 被 `ChatViewModel` 使用构造请求
- 从 `ChatMessage` 转换而来
- 发送给 `ApiService`

#### 38. **Delta.kt**

**功能**：
- SSE流式响应增量数据
- 字段：`content`、`reasoningContent`

**联动关系**：
- 被 `ApiClient` 解析SSE响应
- 通过 `Flow` 发射给 `ChatRepository`
- 被 `ChatViewModel` 接收并拼接

#### 39. **SelectedMedia.kt**

**功能**：
- 附件预览模型
- 字段：`uri`、`type`（IMAGE/FILE）

**联动关系**：
- 被 `PreviewAdapter` 使用
- 被 `BaseAttachmentActivity` 管理
- 用于附件选择和预览

#### 40. **AccountInfo.kt**

**功能**：
- 账户UI模型
- 用于登录界面展示

**联动关系**：
- 被 `AccountViewModel` 使用
- 从 `AccountEntity` 转换而来

---

### 🛠️ 工具类

#### 41. **ModelPreferences.kt**

**功能**：
- SharedPreferences封装
- 保存和读取当前选择的AI模型
- 持久化用户偏好设置

**方法**：
- `saveSelectedModel(context, modelId)`：保存模型选择
- `getSelectedModel(context): ModelConfig`：读取当前模型

**联动关系**：
- 被 `ChatViewModel` 使用
- 被 `WebSearchAndModelFragment` 使用
- 在模型选择时保存，发送消息时读取

#### 42. **DialogHelper.kt**

**功能**：
- 对话框辅助工具类
- 封装常用的对话框显示逻辑
- 提供统一的对话框样式

**联动关系**：
- 被各个 Activity 使用
- 简化对话框创建代码

#### 43. **OCRHelper.kt**（图片文字识别）

**功能**：
- 封装Google ML Kit文本识别
- 支持中英文识别
- 提供两种识别方法：
  - `parseImage(uri)`：从URI识别
  - `parseBitmap(bitmap)`：从Bitmap识别

**识别流程**：
1. 加载图片为Bitmap
2. 创建InputImage对象
3. 调用TextRecognizer识别
4. 提取文本内容
5. 返回识别结果

**联动关系**：
- 被 `ChatViewModel` 使用
- 用于批量OCR识别图片（batchOCRImages）
- 用于PDF文件OCR识别

#### 44. **XunfeiSpeechRecognizer.kt**（语音识别封装）

**功能**：
- 封装科大讯飞语音识别SDK
- 提供简化的API接口
- 管理识别生命周期

**核心配置**：
- APPID：b4d78b8e
- 语言：中文（普通话）
- 前端点超时：4秒
- 后端点超时：1秒
- 自动添加标点符号

**方法**：
- `startListening(callback)`：开始识别
- `stopListening()`：停止识别
- `cancel()`：取消识别
- `destroy()`：释放资源

**联动关系**：
- 被 `BaseAttachmentActivity` 使用
- 提供语音识别结果回调
- 失败时自动回退到Google语音识别

#### 45. **DefaultTopics.kt**

**功能**：
- 默认推荐话题数据
- 提供预设的话题列表
- 用于初始化数据库

**联动关系**：
- 被 `DialogueViewModel` 使用
- 在首次启动时插入话题数据

---

### 📄 布局文件

#### 26. **activity_login.xml**

- MainActivity的布局文件（登录界面）

#### 27. **activity_dialogue.xml**

- DialogueActivity的布局文件（对话列表）

#### 28. **activity_chat.xml**

- ChatActivity的布局文件（聊天界面）

#### 29. **item_history.xml**

- 对话历史列表项布局

#### 30. **item_message_user.xml**

- 用户消息项布局

#### 31. **item_message_ai.xml**

- AI消息项布局

#### 32. **item_model_option.xml**

- 模型选择项布局

#### 33. **dialog_model_selector.xml**

- 模型选择对话框布局

---

## 五、核心业务流程

### 🔄 流程1：用户登录

```
LoginActivity → AccountViewModel.login()
→ AccountRepository.authenticate()
→ AccountDao.query()
→ 验证成功 → 跳转DialogueActivity
```

### 🔄 流程2：主页加载推荐话题

```
DialogueActivity.onCreate()
→ DialogueViewModel.initializeTopics()
→ 检查数据库是否为空
→ 如果为空：插入DefaultTopics数据
→ DialogueViewModel.loadTopics()
→ LiveData更新
→ TopicAdapter展示话题列表
```

### 🔄 流程3：点击话题创建对话

```
DialogueActivity → 用户点击话题
→ HistoryViewModel.createNewConversation(content)
→ ChatRepository.createConversation()
→ ConversationDao.insertConversation()
→ 返回conversationId
→ 跳转ChatActivity（传递conversationId和初始问题）
→ ChatActivity自动发送初始问题
```

### 🔄 流程4：发送消息（完整流程）

```
【用户输入】
InputBoxFragment → 用户输入文本/选择附件
→ 点击发送按钮
→ 回调 InputBoxListener.onSendMessage()

【Activity处理】
ChatActivity.onSendMessage()
→ 如果有附件：ChatViewModel.sendMessageWithAttachments()
→ 如果无附件：ChatViewModel.sendMessage()

【ViewModel处理】
ChatViewModel.sendMessage(content)
→ 检查conversationId（为空则创建新对话）
→ 添加用户消息到列表
→ 保存用户消息到数据库
→ 如果开启联网搜索：
   ├─ SerperWebSearchService.search(query)
   ├─ 获取搜索结果
   └─ 将搜索结果拼接到Prompt
→ 构造ApiMessage列表（包含历史上下文）
→ 添加AI占位消息（isComplete=false）

【网络请求】
ChatRepository.streamChat(messages, modelConfig)
→ ApiClient.streamChat()
→ OkHttpClient发送POST请求
→ 接收SSE流式响应
→ 逐行解析data:行
→ 提取delta.content和delta.reasoning_content
→ 通过Flow发射Delta对象

【流式输出】
ChatViewModel收集Flow
→ 拼接fullResponseBuilder
→ 每4字符批量更新UI
→ 延迟30ms（打字机效果）
→ 更新列表中的AI消息
→ 发射MessageUpdateEvent.ItemChanged
→ ChatMessageAdapter局部刷新
→ 流式输出完成后标记isComplete=true
→ 保存AI消息到数据库
→ 更新ConversationEntity时间戳和消息计数
```

### 🔄 流程5：分页加载历史消息

```
【触发加载】
ChatActivity → 用户下拉到顶部
→ SwipeRefreshLayout触发刷新
→ ChatViewModel.loadMoreHistory()

【查询数据】
ChatViewModel.loadMoreHistory()
→ 获取当前最早消息的timestamp
→ ChatRepository.getMessagesBefore(conversationId, timestamp, 20)
→ MessageDao.getMessagesBefore()
→ 查询数据库（按时间倒序，limit 20）
→ 转换为ChatMessage列表

【更新UI】
→ 插入到列表头部（index 0）
→ 发射MessageUpdateEvent.HistoryLoaded(count)
→ ChatMessageAdapter.notifyItemRangeInserted(0, count)
→ 标记这些消息已播放动画（markMessagesAsAnimated）
→ 滚动到第count个位置（保持用户视图位置）
→ SwipeRefreshLayout停止刷新
```

### 🔄 流程6：语音识别

```
【开始识别】
InputBoxFragment → 用户长按"按住说话"按钮
→ 回调 InputBoxListener.onVoiceRecognitionStart()
→ ChatActivity.onVoiceRecognitionStart()
→ BaseAttachmentActivity.startVoiceRecognition()
→ XunfeiSpeechRecognizer.startListening()
→ 科大讯飞SDK开始识别

【识别结果】
XunfeiSpeechRecognizer → 识别完成
→ 回调 onResult(text)
→ BaseAttachmentActivity.onVoiceRecognitionResult(text)
→ ChatActivity.onVoiceRecognitionResult(text)
→ 自动切换到键盘模式
→ InputBoxFragment.setInputText(text)
→ 用户可编辑后发送

【失败回退】
XunfeiSpeechRecognizer → 初始化失败
→ BaseAttachmentActivity.startGoogleVoiceRecognition()
→ 启动Google语音识别Intent
→ onActivityResult接收结果
→ 同样填入输入框
```

### 🔄 流程7：图片OCR识别

```
【选择图片】
InputBoxFragment → 用户点击附件按钮
→ 选择图片
→ BaseAttachmentActivity.selectImages()
→ 启动图片选择器
→ onActivityResult接收URI列表
→ 添加到selectedMediaList
→ PreviewAdapter展示预览

【发送识别】
InputBoxFragment → 用户点击发送
→ ChatActivity.onSendMessage()
→ ChatViewModel.sendMessageWithAttachments(text, imageUris, fileUris)
→ 显示OCR进度对话框
→ ChatViewModel.batchOCRImages(imageUris)
→ 遍历每张图片：
   ├─ OCRHelper.parseImage(uri)
   ├─ ML Kit识别文字
   ├─ 更新进度（Recognizing(current, total)）
   └─ 返回识别结果列表
→ 拼接识别内容：
   图片1识别内容：
   [识别文字]
   
   图片2识别内容：
   [识别文字]
→ 调用sendMessage(finalContent)
→ 清空附件列表
→ 显示识别成功提示
```

### 🔄 流程8：切换AI模型

```
WebSearchAndModelFragment → 用户点击模型选择
→ 显示模型选择对话框
→ ModelAdapter展示ModelRegistry.ALL_MODELS
→ 用户选择模型
→ 回调 WebSearchAndModelListener.onModelSwitch(modelConfig)
→ ChatActivity.onModelSwitch(modelConfig)
→ ChatViewModel.switchModel(modelConfig)
→ 更新_currentModel LiveData
→ ModelPreferences.saveSelectedModel(modelId)
→ 下次发送消息时使用新模型
```

### 🔄 流程9：消息操作（点赞/重新生成）

```
【长按消息】
ChatMessageAdapter → 用户长按AI消息
→ 回调 onAiMessageLongClick(message, anchorView, x, y)
→ ChatActivity.showMessageActionsDialog()
→ MessageActionsBottomSheet.show()

【点赞操作】
MessageActionsBottomSheet → 用户点击点赞
→ 回调 Listener.onLike(message)
→ ChatActivity.onLike(message)
→ ChatActivity.updateLikeState(message, isLike=true)
→ 更新内存中的消息对象（isLiked=true, isDisliked=false）
→ ChatMessageAdapter.notifyItemChanged(index)
→ ChatViewModel.updateMessageLikeState(message)
→ ChatRepository.updateMessageLikeState()
→ MessageDao.updateMessageLikeState()
→ 持久化到数据库

【重新生成】
MessageActionsBottomSheet → 用户点击重新生成
→ 回调 Listener.onReload(message)
→ ChatActivity.onReload(message)
→ ChatActivity.reloadAnswer(message)
→ 找到上一条用户消息
→ ChatViewModel.sendMessage(userQuestion)
→ 重新发起请求（流程同流程4）
```

---

## 六、架构优势与特点

### ✅ 核心优势

#### 1. **清晰的分层架构**
- **UI层**：Activity + Fragment组件化，职责单一
- **ViewModel层**：业务逻辑独立，支持数据共享
- **Repository层**：数据源抽象，统一数据访问
- **Data层**：Room数据库 + 网络请求，数据持久化

#### 2. **Fragment组件化设计**
- **InputBoxFragment**：输入框独立管理，支持复用
- **HistoryFragment**：历史列表独立管理，数据共享
- **WebSearchAndModelFragment**：搜索和模型选择独立
- **优势**：解耦UI组件，提高可维护性和可测试性

#### 3. **流式响应优化**
- **批量更新**：每4字符更新一次UI，减少渲染压力
- **延迟控制**：30ms延迟，保持流畅度（约30fps）
- **局部刷新**：使用PAYLOAD_CONTENT_UPDATE精确更新
- **性能优化**：避免notifyDataSetChanged全量刷新

#### 4. **数据持久化设计**
- **多表关联**：Conversation ↔ Message ↔ Attachment
- **分页加载**：支持历史消息分页查询（每次20条）
- **状态持久化**：点赞、点踩状态保存到数据库
- **离线支持**：本地数据库支持离线使用

#### 5. **多模态输入支持**
- **图片OCR**：ML Kit文本识别（中英文）
- **文件解析**：PDF（PdfRenderer + OCR）、DOCX（ZIP解析）、TXT
- **批量处理**：支持多图片/文件同时上传
- **进度反馈**：OCRProgress状态机管理识别进度

#### 6. **双引擎语音识别**
- **主引擎**：科大讯飞SDK（更准确，支持离线）
- **备用引擎**：Google语音识别（兼容性好）
- **智能回退**：主引擎失败自动切换备用
- **生命周期管理**：自动释放资源，避免内存泄漏

#### 7. **联网搜索增强**
- **集成Serper API**：实时搜索互联网信息
- **上下文增强**：搜索结果作为Prompt上下文
- **格式化输出**：Markdown格式展示搜索结果
- **开关控制**：用户可自由开启/关闭

#### 8. **响应式编程**
- **LiveData**：自动通知UI更新，生命周期感知
- **Coroutines + Flow**：异步处理，流式数据
- **MessageUpdateEvent**：精确通知UI更新类型
- **避免内存泄漏**：自动管理订阅生命周期

#### 9. **性能优化**
- **RecyclerView优化**：
  - `setHasFixedSize(true)`：固定大小优化
  - `setItemViewCacheSize(20)`：增加缓存
  - 关闭动画：`supportsChangeAnimations = false`
- **Markwon渲染优化**：Activity级别单例，避免重复创建
- **假数据生成**：debugSeedFakeConversation测试长列表性能

#### 10. **模型可扩展**
- **ModelRegistry**：统一管理所有AI模型
- **ModelConfig**：灵活配置模型参数
- **动态切换**：运行时切换模型，无需重启
- **持久化**：SharedPreferences保存用户选择

### ⚠️ 注意事项与改进方向

#### 1. **数据库迁移策略**
- **当前问题**：使用 `fallbackToDestructiveMigration`，升级会清空数据
- **改进方向**：实现正确的Migration策略，保留用户数据

#### 2. **内存管理**
- **当前问题**：长列表可能导致内存占用过高
- **改进方向**：
  - 实现消息分页加载（已部分实现）
  - 限制内存中的消息数量
  - 使用DiffUtil优化列表更新

#### 3. **网络异常处理**
- **当前问题**：网络中断、超时处理不够完善
- **改进方向**：
  - 统一异常处理机制
  - 用户友好的错误提示
  - 自动重试机制

#### 4. **conversationId管理**
- **当前问题**：conversationId传递逻辑复杂
- **改进方向**：
  - 使用ViewModel共享conversationId
  - 简化Activity间传递逻辑

#### 5. **ViewModel生命周期**
- **当前问题**：HistoryViewModel在多个Activity中的实例管理
- **改进方向**：
  - 使用Application级别的ViewModel
  - 或使用Navigation Component统一管理

#### 6. **测试覆盖**
- **当前问题**：缺少单元测试和UI测试
- **改进方向**：
  - Repository层单元测试
  - ViewModel层单元测试
  - UI自动化测试

#### 7. **代码高亮**
- **当前问题**：Markwon代码高亮暂时禁用（性能考虑）
- **改进方向**：
  - 优化Prism4j配置
  - 异步渲染代码块
  - 限制高亮代码块数量

---

## 七、技术栈总结

### 📚 核心技术

| 技术 | 版本/库 | 用途 |
|------|---------|------|
| **语言** | Kotlin 1.9.24 | 主要开发语言 |
| **架构** | MVVM | 架构模式 |
| **UI框架** | ViewBinding | 视图绑定 |
| **异步处理** | Coroutines 1.7.3 + Flow | 协程和流式数据 |
| **响应式编程** | LiveData | 数据观察 |
| **数据库** | Room 2.6.1 | 本地数据持久化 |
| **网络请求** | OkHttp 4.12.0 | HTTP客户端 |
| **JSON解析** | Gson 2.10.1 | JSON序列化/反序列化 |
| **Markdown渲染** | Markwon 4.6.2 | Markdown展示 |
| **代码高亮** | Prism4j 2.0.0 | 代码语法高亮 |
| **图片加载** | Glide 4.16.0 | 图片加载和缓存 |
| **OCR识别** | ML Kit 16.0.0 | 文本识别 |
| **语音识别** | 科大讯飞SDK | 语音转文字 |
| **构建工具** | Gradle 8.6.0 | 项目构建 |

### 🎨 UI组件

- **Material Design**：Material 1.10.0
- **RecyclerView**：列表展示
- **SwipeRefreshLayout**：下拉刷新
- **DrawerLayout**：侧边栏
- **BottomSheet**：底部弹窗
- **Fragment**：组件化UI

### 🔧 开发工具

- **Android Studio**：IDE
- **Kotlin Kapt**：注解处理器
- **ProGuard**：代码混淆

---

## 八、项目亮点

### 🌟 技术亮点

1. **Fragment组件化架构**
   - 输入框、历史列表、搜索模型独立Fragment
   - 高复用性，易于维护

2. **流式响应优化**
   - SSE流式数据处理
   - 批量更新 + 延迟控制
   - 打字机效果流畅自然

3. **多模态输入**
   - 文本、图片OCR、文件解析
   - 批量处理，进度反馈
   - 支持PDF、DOCX、TXT等格式

4. **双引擎语音识别**
   - 科大讯飞 + Google双引擎
   - 智能回退机制
   - 生命周期管理完善

5. **性能优化**
   - RecyclerView缓存优化
   - 局部刷新机制
   - 分页加载历史消息
   - Markwon单例复用

6. **联网搜索增强**
   - 集成Serper搜索API
   - 上下文增强AI回答
   - Markdown格式化展示

### 🎯 功能亮点

1. **完整的对话管理**
   - 创建、重命名、删除、置顶
   - 搜索历史对话
   - 多会话隔离

2. **丰富的消息操作**
   - 复制、分享、点赞、点踩
   - 重新生成回答
   - 长按弹出操作菜单

3. **灵活的模型切换**
   - 支持10+种AI模型
   - 运行时动态切换
   - 持久化用户选择

4. **推荐话题系统**
   - 快速开始对话
   - 默认话题数据
   - 可扩展话题库

5. **假数据生成**
   - 测试长列表性能
   - 快速生成120轮对话
   - 包含Markdown、代码块等

---

## 九、总结

这是一个基于**MVVM架构**的**现代化AI对话应用**，采用**Fragment组件化设计**，具备完整的对话会话管理系统。

### 核心特点

1. **架构清晰**：UI、ViewModel、Repository、Data四层分离，职责明确
2. **组件化设计**：Fragment独立管理，高复用性
3. **流式响应**：SSE + Flow实现打字机效果，用户体验流畅
4. **多模态支持**：文本、图片OCR、文件解析、语音识别
5. **性能优化**：批量更新、局部刷新、分页加载
6. **功能完善**：对话管理、消息操作、模型切换、联网搜索

### 技术优势

- ✅ **Kotlin Coroutines + Flow**：异步处理和流式数据
- ✅ **Room数据库**：多表关联，分页查询
- ✅ **Markwon渲染**：支持Markdown、代码高亮、表格等
- ✅ **ML Kit OCR**：图片文字识别
- ✅ **科大讯飞SDK**：高精度语音识别
- ✅ **Serper搜索**：联网搜索增强AI回答

### 可扩展性

- 🔧 **模块化设计**：易于添加新功能
- 🔧 **接口抽象**：易于替换实现
- 🔧 **配置化**：模型、参数可配置
- 🔧 **预留扩展**：Attachment表预留多模态支持

### 适用场景

- 📱 **AI对话应用**：通用AI聊天客户端
- 📚 **知识问答**：企业知识库问答
- 🎓 **教育辅助**：学习助手、作业辅导
- 💼 **办公助手**：文档解析、内容生成

---

**项目架构设计遵循SOLID原则，代码结构清晰，易于维护和扩展。**
