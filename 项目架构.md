# 项目架构文档

## 一、架构概述

### 1. 架构模式
- **MVVM 架构**（Model-View-ViewModel）
- 数据层：本地数据库（Room）+ 网络请求（Retrofit + OkHttp）
- UI 层：Activity + RecyclerView + ViewBinding
- 响应式编程：LiveData + Coroutines

### 2. 核心功能模块
- 用户认证（登录/注册）
- 对话会话管理（创建、历史记录、删除、置顶）
- 实时消息展示（流式响应、打字机效果）
- 多模型切换（豆包、DeepSeek等）
- 数据持久化（Room数据库）
- 离线同步支持

---

## 二、项目目录结构

```
com.example.myapplication/
├── ui/                          # UI层 - Activity界面
│   ├── MainActivity.kt          # 登录/启动界面
│   ├── DialogueActivity.kt      # 对话列表（历史记录）
│   └── ChatActivity.kt          # 聊天对话界面
│
├── viewmodel/                   # ViewModel层 - 业务逻辑
│   ├── LoginViewModel.kt        # 登录业务逻辑
│   ├── DialogueViewModel.kt     # 对话列表业务逻辑
│   └── ChatViewModel.kt         # 聊天业务逻辑
│
├── repository/                  # Repository层 - 数据仓库
│   ├── UserRepository.kt        # 用户数据仓库
│   ├── HistoryRepository.kt     # 历史记录数据仓库
│   ├── ChatRepository.kt        # 聊天数据仓库
│   └── ChatRepositoryOptimized.kt # 优化版聊天仓库
│
├── adapter/                     # Adapter - RecyclerView适配器
│   ├── HistoryAdapter.kt        # 历史对话列表适配器
│   ├── ChatMessageAdapter.kt    # 聊天消息列表适配器
│   └── ModelAdapter.kt          # 模型选择适配器
│
├── data/db/                     # 数据层 - 本地数据库
│   ├── AppDatabase.kt           # Room数据库
│   ├── MessageEntity.kt         # 消息实体
│   ├── MessageDao.kt            # 消息数据访问对象
│   ├── ConversationEntity.kt    # 对话会话实体
│   └── ConversationDao.kt       # 对话数据访问对象
│
├── network/                     # 网络层 - API请求
│   ├── ApiService.kt            # Retrofit API接口定义
│   └── ApiClient.kt             # OkHttp客户端配置
│
├── model/                       # 数据模型
│   ├── ChatMessage.kt           # 聊天消息模型
│   ├── ChatHistory.kt           # 对话历史模型
│   ├── ChatModels.kt            # AI模型定义
│   └── ModelConfig.kt           # 模型配置
│
└── utils/                       # 工具类
    └── ModelPreferences.kt      # SharedPreferences工具（模型选择持久化）
```

---

## 三、文件功能详解及联动关系

### 📱 UI层 (Activity)

#### 1. **MainActivity.kt**
**功能**：
- 应用启动入口和登录界面
- 用户登录/注册功能
- 用户认证成功后跳转到DialogueActivity

**联动关系**：
- 使用 `LoginViewModel` 处理登录业务逻辑
- 通过 `UserRepository` 验证用户凭证
- 登录成功跳转 → `DialogueActivity`

---

#### 2. **DialogueActivity.kt**
**功能**：
- 对话历史列表主界面
- 显示所有历史对话会话
- 创建新对话、删除对话、置顶对话
- 模型选择对话框

**联动关系**：
- 使用 `DialogueViewModel` 管理对话列表数据
- 通过 `HistoryRepository` 从数据库获取历史记录
- 使用 `HistoryAdapter` 展示对话列表
- 使用 `ModelAdapter` 在对话框中展示可选模型
- 点击对话项跳转 → `ChatActivity`（传递conversationId）
- 点击创建新对话 → `ChatActivity`（创建新conversationId）

---

#### 3. **ChatActivity.kt**
**功能**：
- 聊天对话界面
- 显示用户和AI的消息记录
- 发送消息给AI并接收流式响应
- 打字机效果展示AI回复
- 加载历史消息记录

**联动关系**：
- 使用 `ChatViewModel` 管理消息数据和发送逻辑
- 通过 `ChatRepository` 发送网络请求和保存消息
- 使用 `ChatMessageAdapter` 展示消息列表
- 接收 `DialogueActivity` 传递的 `conversationId`
- 自动加载该会话的历史消息
- 发送消息后更新 `ConversationEntity` 的最后更新时间

---

### 🧠 ViewModel层

#### 4. **LoginViewModel.kt**
**功能**：
- 登录表单验证
- 用户认证逻辑
- 登录状态管理

**联动关系**：
- 依赖 `UserRepository` 进行用户认证
- 通过 LiveData 向 `MainActivity` 发送登录结果

---

#### 5. **DialogueViewModel.kt**
**功能**：
- 管理对话历史列表数据
- 创建、删除、置顶对话
- 管理当前选择的AI模型

**联动关系**：
- 依赖 `HistoryRepository` 获取对话列表
- 通过 LiveData 向 `DialogueActivity` 推送数据更新
- 创建新对话时生成新的 `ConversationEntity`

---

#### 6. **ChatViewModel.kt**
**功能**：
- 管理当前会话的消息列表
- 发送消息并处理流式响应
- 打字机效果逻辑
- 加载历史消息
- 管理conversationId

**联动关系**：
- 依赖 `ChatRepository` 发送消息和保存数据
- 通过 LiveData 向 `ChatActivity` 推送消息更新
- 在发送消息时关联 `conversationId`
- 调用 `loadInitialMessages()` 从数据库加载历史消息

---

### 💾 Repository层 (数据仓库)

#### 7. **UserRepository.kt**
**功能**：
- 用户认证业务逻辑
- 用户数据管理
- （可能）与后端API交互验证用户

**联动关系**：
- 被 `LoginViewModel` 调用
- 可能调用 `ApiService` 进行网络认证

---

#### 8. **HistoryRepository.kt**
**功能**：
- 对话历史记录的增删改查
- 从Room数据库获取对话列表
- 创建、删除、更新对话会话

**联动关系**：
- 依赖 `ConversationDao` 访问数据库
- 被 `DialogueViewModel` 调用
- 返回 `List<ChatHistory>` 数据模型

---

#### 9. **ChatRepository.kt**
**功能**：
- 发送消息到AI后端
- 接收流式响应（SSE）
- 保存消息到Room数据库
- 管理消息的conversationId关联

**联动关系**：
- 依赖 `ApiClient` 和 `ApiService` 发送网络请求
- 依赖 `MessageDao` 保存消息到数据库
- 依赖 `ConversationDao` 更新对话时间戳
- 被 `ChatViewModel` 调用
- 将网络响应转换为 `ChatMessage` 模型

---

#### 10. **ChatRepositoryOptimized.kt**
**功能**：
- ChatRepository的优化版本
- 可能包含性能优化、缓存逻辑等

**联动关系**：
- 与 `ChatRepository` 功能类似
- 可能被 `ChatViewModel` 使用（替代方案）

---

### 🎨 Adapter层

#### 11. **HistoryAdapter.kt**
**功能**：
- RecyclerView适配器，展示对话历史列表
- 绑定 `item_history.xml` 布局
- 处理列表项点击事件

**联动关系**：
- 被 `DialogueActivity` 使用
- 数据源来自 `DialogueViewModel`
- 点击事件触发跳转到 `ChatActivity`

---

#### 12. **ChatMessageAdapter.kt**
**功能**：
- RecyclerView适配器，展示聊天消息
- 区分用户消息和AI消息（不同布局）
- 支持打字机效果更新
- 绑定 `item_message_user.xml` 和 `item_message_ai.xml`

**联动关系**：
- 被 `ChatActivity` 使用
- 数据源来自 `ChatViewModel` 的消息列表
- 实时更新AI消息内容（流式响应）

---

#### 13. **ModelAdapter.kt**
**功能**：
- RecyclerView适配器，展示可选AI模型
- 绑定 `item_model_option.xml` 布局
- 处理模型选择事件

**联动关系**：
- 被 `DialogueActivity` 的模型选择对话框使用
- 数据源来自 `ChatModels.kt` 的模型列表
- 选择结果保存到 `ModelPreferences`

---

### 🗄️ 数据库层

#### 14. **AppDatabase.kt**
**功能**：
- Room数据库实例（单例）
- 定义数据库版本和迁移策略
- 提供DAO访问接口

**联动关系**：
- 包含 `MessageDao` 和 `ConversationDao`
- 被所有Repository调用获取DAO实例
- 当前版本：v2（支持消息和对话表）

---

#### 15. **MessageEntity.kt**
**功能**：
- 消息表的实体类
- 字段：id, conversationId, content, isUser, timestamp

**联动关系**：
- 被 `MessageDao` 使用
- 与 `ConversationEntity` 通过 `conversationId` 关联
- 被 `ChatRepository` 保存到数据库

---

#### 16. **MessageDao.kt**
**功能**：
- 消息表的数据访问对象（DAO）
- 提供插入、查询、删除消息的方法
- 支持按conversationId查询消息

**联动关系**：
- 被 `ChatRepository` 调用
- 操作 `MessageEntity`
- 查询结果用于加载历史消息

---

#### 17. **ConversationEntity.kt**
**功能**：
- 对话会话表的实体类
- 字段：id, title, createdAt, lastMessageAt, isPinned

**联动关系**：
- 被 `ConversationDao` 使用
- 与 `MessageEntity` 是一对多关系
- 被 `HistoryRepository` 和 `ChatRepository` 操作

---

#### 18. **ConversationDao.kt**
**功能**：
- 对话会话表的数据访问对象（DAO）
- 提供创建、删除、更新、查询对话的方法
- 支持按时间排序、置顶排序

**联动关系**：
- 被 `HistoryRepository` 和 `ChatRepository` 调用
- 操作 `ConversationEntity`
- 查询结果用于展示对话列表

---

### 🌐 网络层

#### 19. **ApiService.kt**
**功能**：
- Retrofit接口定义
- 定义与AI后端的API端点
- 支持流式响应（SSE）

**联动关系**：
- 被 `ApiClient` 创建实例
- 被 `ChatRepository` 调用发送消息
- 接收用户消息，返回AI响应流

---

#### 20. **ApiClient.kt**
**功能**：
- OkHttpClient配置
- 设置超时、拦截器、日志
- 创建Retrofit实例

**联动关系**：
- 提供 `ApiService` 的实例
- 被 `ChatRepository` 使用
- 配置SSE流式响应的连接参数

---

### 📦 Model层

#### 21. **ChatMessage.kt**
**功能**：
- 聊天消息的UI数据模型
- 包含消息内容、发送者、时间戳

**联动关系**：
- 被 `ChatViewModel` 和 `ChatMessageAdapter` 使用
- 从 `MessageEntity` 转换而来
- 用于UI展示

---

#### 22. **ChatHistory.kt**
**功能**：
- 对话历史的UI数据模型
- 包含对话标题、最后消息时间、消息预览

**联动关系**：
- 被 `DialogueViewModel` 和 `HistoryAdapter` 使用
- 从 `ConversationEntity` + `MessageEntity` 聚合而来
- 用于历史列表展示

---

#### 23. **ChatModels.kt**
**功能**：
- 定义可用的AI模型列表
- 包含模型ID、名称、描述等信息

**联动关系**：
- 被 `ModelAdapter` 使用
- 被 `DialogueViewModel` 读取
- 用户选择的模型保存到 `ModelPreferences`

---

#### 24. **ModelConfig.kt**
**功能**：
- AI模型配置类
- 可能包含模型参数、温度、max_tokens等

**联动关系**：
- 被 `ChatRepository` 使用构造请求参数
- 与 `ChatModels.kt` 配合使用

---

### 🛠️ 工具类

#### 25. **ModelPreferences.kt**
**功能**：
- SharedPreferences封装
- 保存和读取当前选择的AI模型
- 持久化用户偏好设置

**联动关系**：
- 被 `DialogueViewModel` 和 `ChatViewModel` 使用
- 在模型选择对话框中保存选择
- 发送消息时读取当前模型

---

### 📄 布局文件

#### 26. **activity_login.xml**
- MainActivity的布局文件（登录界面）

#### 27. **activity_dialogue.xml**
- DialogueActivity的布局文件（对话列表）

#### 28. **activity_chat.xml**
- ChatActivity的布局文件（聊天界面）

#### 29. **item_history.xml**
- 对话历史列表项布局

#### 30. **item_message_user.xml**
- 用户消息项布局

#### 31. **item_message_ai.xml**
- AI消息项布局

#### 32. **item_model_option.xml**
- 模型选择项布局

#### 33. **dialog_model_selector.xml**
- 模型选择对话框布局

---

## 四、核心业务流程

### 🔄 流程1：用户登录
```
MainActivity → LoginViewModel → UserRepository → 验证成功 → 跳转DialogueActivity
```

### 🔄 流程2：创建新对话
```
DialogueActivity → 点击新建 → DialogueViewModel.createConversation()
→ HistoryRepository.createConversation() → ConversationDao.insert()
→ 跳转ChatActivity（传递conversationId）
```

### 🔄 流程3：发送消息
```
ChatActivity → 用户输入 → ChatViewModel.sendMessage()
→ ChatRepository.sendMessage(conversationId, content)
→ ApiService发送请求 → 接收流式响应
→ MessageDao.insert() 保存消息
→ ConversationDao.updateTimestamp() 更新对话时间
→ LiveData通知UI更新 → ChatMessageAdapter刷新
```

### 🔄 流程4：加载历史消息
```
ChatActivity.onCreate() → ChatViewModel.loadInitialMessages(conversationId)
→ MessageDao.getMessagesByConversation(conversationId)
→ 转换为ChatMessage列表 → LiveData更新 → RecyclerView展示
```

### 🔄 流程5：切换AI模型
```
DialogueActivity → 点击模型选择按钮 → 显示对话框
→ ModelAdapter展示ChatModels列表 → 用户选择
→ ModelPreferences.saveSelectedModel() → 持久化保存
→ 下次发送消息时使用新模型
```

---

## 五、架构特点

### ✅ 优势
1. **清晰的分层架构**：UI、ViewModel、Repository、Data分离
2. **数据持久化**：Room数据库支持离线使用
3. **流式响应**：实时展示AI回复（打字机效果）
4. **会话管理**：每条消息关联conversationId，支持多会话隔离
5. **响应式编程**：LiveData自动通知UI更新
6. **模型可切换**：支持多种AI模型动态选择

### ⚠️ 注意事项
1. **数据库版本管理**：当前使用 `fallbackToDestructiveMigration`，升级会清空数据
2. **内存管理**：ChatMessageAdapter需正确处理列表引用，避免RecyclerView不一致
3. **网络异常处理**：需要处理网络中断、超时等情况
4. **conversationId传递**：确保在Activity间正确传递和设置

---

## 六、待优化方向

1. **数据库迁移策略**：实现正确的Migration，避免数据丢失
2. **错误处理**：统一的异常处理和用户提示机制
3. **性能优化**：大量消息时的分页加载
4. **UI优化**：Markdown渲染、代码高亮等
5. **功能扩展**：文件上传、语音输入、多模态支持
6. **测试覆盖**：单元测试和UI测试

---

## 七、总结

这是一个基于MVVM架构的AI对话应用，具备完整的对话会话管理系统。核心特点是通过conversationId将消息与对话关联，实现了多会话隔离和历史记录管理。各层职责清晰，通过Repository模式隔离数据源，使用LiveData实现响应式UI更新。
